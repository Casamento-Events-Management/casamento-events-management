# Gemini Primary Model Workspace Directives
This workspace uses **Gemini** as the primary execution model.
## Primary Directives
- Strict adherence to workspace rules in AGENTS.md.
- Prioritize Next.js 15+ App Router Server Components and SEO performance optimization.
- Enforce strict TypeScript typing (`no implicit any`, explicit return types for public helpers).
- Always enforce video thumbnail interaction rules (no autoplay) to optimize LCP and Core Web Vitals.
- Ensure all business logic and payment integrations remain modular, decoupled, and secure.